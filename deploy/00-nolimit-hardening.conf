# No Limit production SSH policy.
# Loaded before OpenCloudOS cloud-init's 50-cloud-init.conf because OpenSSH
# uses the first value it obtains for each keyword.
PasswordAuthentication no
KbdInteractiveAuthentication no
PermitRootLogin prohibit-password
PubkeyAuthentication yes
MaxAuthTries 3
