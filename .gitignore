# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Secrets / local env — never commit
.env
.env.*
!.env.example

# Scratch verification artifacts (screenshots, throwaway scripts) at repo root —
# never commit these. Real assets live in public/ or src/.
/_*.png
/_*.mjs
/*verify*.png
/debug_*.png
/coach_mobile_*.png
/nl_live_*.png
/nl_verify_*.png
/builder_density_*.png
test-results/

# Brand source package (32MB PDF, mockups, font zips) — web-ready assets are
# exported into public/; the package itself stays local only
branding/
# Vite bundles only the approved 48-degree masters below.
!branding/
branding/*
!branding/Source vector files/
branding/Source vector files/*
!branding/Source vector files/NX LIMIT Training Lean 48 Trial.svg
!branding/Source vector files/NX LIMIT Small-Use Lean 48 Trial.svg

# Local data dumps (real client data — never commit)
etl-data.json
*.dump.json
public/pose/

# Purchased anatomy vector pack (33MB, licensed to Kent personally) — the
# muscle diagram's shape/coordinate data was extracted from this into
# src/muscleGroups.ts and src/assets/muscleOutline{Front,Back}.png; those
# processed outputs ship, this source package stays local only.
Anatomy Vectors/
jv-tmp/
pose-series-*.json
